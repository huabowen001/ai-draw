const CONFIG = {
    apiBaseUrl: 'http://localhost:8080/api/v1',
    drawioUrl: 'https://embed.diagrams.net',
    maxXmlPreviewLength: 10000,
    toastDuration: 3000,
    debounceDelay: 300
};

Object.freeze(CONFIG);

export default CONFIG;
